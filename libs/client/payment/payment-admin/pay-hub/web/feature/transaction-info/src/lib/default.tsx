/* eslint-disable-next-line */
export interface Default {}

export function Default(props: Default) {
    return (
        <div>
            <h1>Welcome to Default!</h1>
        </div>
    );
}

export default Default;
